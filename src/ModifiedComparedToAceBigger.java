public class ModifiedComparedToAceBigger {

    // compareTo method
/*    public int compareTo (Card that) {
        int thisRank = this.rank; // modified based on Ex. 12.2 to cover rank of Ace > King
        int thatRank = that.rank;

        if (thisRank == 1) {
            thisRank += 13;
        }
        if (thatRank == 1) {
            thatRank += 13;
        }
        if (this.suit > that.suit) {
            return 1;
        }
        if (this.suit < that.suit) {
            return -1;
        }
        if (thisRank > thatRank) {
            return 1;
        }
        if (thisRank < thatRank) {
            return -1;
        }
        return 0;
    }*/
}
